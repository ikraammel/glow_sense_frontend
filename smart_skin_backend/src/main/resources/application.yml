spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/smartskin?useSSL=false&serverTimezone=UTC
    username: root
    password: ikram
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect